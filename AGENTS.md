# Natives agent instructions

- Read `docs/standards/README.md` and the relevant standard before coding.
- Performance changes MUST follow `docs/standards/technical/04-performance.md`.
- Use `rtk` for shell commands.
- Run `npm run typecheck`, `npm run lint`, tests, and `npm run perf:check` before handoff.
