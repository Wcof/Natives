// ── Legacy module: env profile CRUD and encryption ──
// These operations are now handled by src-tauri/env_manager.rs (AES-256-GCM)
// and src-tauri/commands/env.rs (IPC commands).
// This stub keeps import references happy; active code routes through
// window.nativesAPI via tauri-adapter.
