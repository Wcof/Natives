# Natives2 — AtomCode 项目指令

## 入口优先级

本文件是项目级最高优先级指令。冲突时以 `docs/standards/` 为唯一权威约束源。

## 强制工具使用

### RTK — 所有 shell 命令必须加 `rtk` 前缀

```bash
# ❌ 错误
git status && npm run build
cargo check

# ✅ 正确
rtk git status && rtk npm run build
rtk cargo check
```

**常用命令速查：**

| 场景 | 命令 | 说明 |
|------|------|------|
| 类型检查 (TS) | `rtk tsc --noEmit` | 替代 `npm run typecheck`，83% 节省 |
| 搜索 | `rtk grep "<pattern>" src` | 替代纯 ripgrep，70-75% 节省 |
| Git | `rtk git status/diff/log` | 59-80% 节省 |
| 测试 (TS) | `rtk npm test` | 90-99% 节省 |
| Rust 检查 | `cd src-tauri && rtk cargo check` | |
| Rust 测试 | `cd src-tauri && rtk cargo test` | |

### CodeGraph — 代码搜索优先工具

仓库含 `.codegraph/` 目录时，**搜索/定位代码必须优先使用 CodeGraph**，而非 grep/find/read。

```bash
codegraph explore "<查询>"   # 语义化代码搜索
codegraph node <符号或文件路径>  # 符号关系查询
```

仅在 `.codegraph/` 不存在时回退到标准 grep/find/read。

### Ponytail

暂未在项目或系统中找到 Ponytail 的引用。如为独立工具，请提供安装方式或路径以补充配置。

## 验证命令

```bash
# 前端
rtk tsc --noEmit              # TypeScript 类型检查
rtk npm run lint              # ESLint
rtk npm test                  # 单元测试

# 后端
cd src-tauri && rtk cargo check  # Rust 类型检查
cd src-tauri && rtk cargo test   # Rust 测试

# 全量构建
rtk npm run build
rtk npm run tauri:build
```

## 项目结构简览

```
src-tauri/           # Tauri Rust 后端（IPC 命令、SQLite、HTTP、终端）
src/                 # Next.js 前端
docs/standards/      # ★ 权威约束源（必读）
```

## 关键约束（详见 CLAUDE.md 及 docs/standards/）

- **禁止假数据**: 每个用户可见字段必须有真实数据源
- **i18n 同步**: UI 文本变更必须同步更新 `en.ts` 和 `zh.ts`
- **凭据加密**: AES-256-GCM（`env_manager.rs`）
- **iframe 沙箱**: `allow-scripts allow-forms`，无 `allow-same-origin`
- **前端后端通信**: 仅通过 `window.nativesAPI`（`tauri-adapter.ts`）

## 功能域架构文档

涉及以下功能域时，必须先读对应的架构文档：

| 功能域 | 文档 | 核心约束 |
|--------|------|----------|
| **模块管理 / 创意工坊** | [`docs/architecture/module-workshop-kernel-runtime.md`](docs/architecture/module-workshop-kernel-runtime.md) | 5 大内核不变量 KI-1~KI-5 |
