[package]
name = "app-host-support"
version = "0.1.0"
edition = "2021"
authors = ["Natives"]
description = "Official managed app runtime support: Native framing, origin gate, runtime locks, session auth, App v1 protocol (ADR-0027 / managed-app-contract v1)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
