[package]
name = "contract-linter"
version = "0.1.0"
description = "Natives Contract Linter — KI-3 static audit for AI-generated Workshop modules, shared by the Tauri host and the Agent Daemon"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
