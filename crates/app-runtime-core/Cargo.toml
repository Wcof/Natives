[package]
name = "app-runtime-core"
version = "0.1.0"
edition = "2021"
authors = ["Natives"]
description = "Built-in App Runtime Core: Native framing, origin gate, runtime locks, session auth, protocol v2 & module contract"

[dependencies]
getrandom = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
