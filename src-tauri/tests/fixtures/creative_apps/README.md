# Creative App fixtures — static assets for unit tests
# These files are never executed by Docker in default CI.

compose_simple/
  docker-compose.yml
  natives.app.json

compose_build_only/
  docker-compose.yml

compose_multi_service/
  compose.yaml

run_manifest/
  natives.app.json

zip_slip/
  (generated in tests)

no_signal/
  README.md
