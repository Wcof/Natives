# Natives engineering instructions

Read `docs/standards/README.md` before changing product code.
Performance-sensitive work MUST also follow `docs/standards/technical/04-performance.md`.
Run `npm run perf:check` for performance changes; do not merge with a failed budget.
